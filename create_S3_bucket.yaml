---
- name: Create empty bcuket
  hosts: localhost
  become: yes
  become_method: sudo
  tasks:
    - name: Create an empty bucket
      s3_bucket:
        bucket: "{{bucket}}"
        mode: "{{mode}}"
    